pre-commit:
  parallel: false
  commands:
    biome-check:
      glob: "src/**/*.ts"
      run: pnpm biome check --write {staged_files}
      stage_fixed: true
    check-types:
      run: pnpm check-types
    test:
      run: pnpm test
